version: '3'
services:
  db:
    image: postgres:17
    restart: always
    volumes:
      - ./data/db:/var/lib/postgresql/data
    ports:
      - 5432:5432
    environment:
      - POSTGRES_DB=betterauthdb
      - POSTGRES_USER=root
      - POSTGRES_PASSWORD=root
